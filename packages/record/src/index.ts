import {
  record,
  getWindowScroll,
  getWindowHeight,
  getWindowWidth,
} from 'rrweb';

import { EventType, IncrementalSource, MouseInteractions } from '@rrweb/types';

export {
  EventType,
  IncrementalSource,
  MouseInteractions,
  record,
  getWindowScroll,
  getWindowHeight,
  getWindowWidth,
};
